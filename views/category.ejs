<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Categories - RishiVerse</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-color: rgb(251, 248, 241);
            font-family: Arial, sans-serif;
        }
    </style>
</head>
<body class="p-4">
    <%- include('partials/layout') %> <!-- Include Navbar -->
    <main class="flex-1 ml-20 p-8">
    <h1 class="text-4xl font-bold mb-8 text-center text-gray-800">Explore Books by Category</h1>

    <% if (categories.length > 0) { %>
        <% categories.forEach(category => { %>
            <div class="mb-10">
                <h2 class="text-2xl font-semibold mb-4 bg-red-800 p-3 rounded-md text-center shadow-lg">
                    <%= category.name %> 📚
                </h2>

                <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
                    <% category.books.forEach(book => { %>
                        <div class="bg- p-4 shadow-md rounded-lg border border-gray-300 transform transition duration-300 hover:scale-105">
                            <h3 class="text-lg font-semibold text-gray-800"><%= book.title %></h3>
                            <p class="text-gray-600 text-sm">by <strong><%= book.author %></strong></p>
                            <p class="text-gray-500 text-sm line-clamp-2 mt-1"><%= book.description %></p>

                            <div class="mt-4 flex justify-between items-center">
                                <a href="/book/<%= book._id %>" class="text-black-500 hover:underline">Read More</a>
                                <a href="<%= book.contentUrl %>" target="_blank" class="bg-red-800 text-white px-3 py-1 rounded-md hover:bg-red-300">
                                    📖 Open PDF
                                </a>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        <% }) %>
    <% } else { %>
        <p class="text-red-500 text-center text-xl">No categories found.</p>
    <% } %>
</body>
</html>
