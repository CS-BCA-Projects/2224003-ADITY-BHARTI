<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= category %> Books</title>
    <style>
        body { font-family: Arial, sans-serif; background-color: #fef4d4; text-align: center; }
        .container { max-width: 800px; margin: auto; padding: 20px; }
        .book-card { background: white; padding: 15px; margin: 10px; border-radius: 5px; text-align: left; box-shadow: 2px 2px 10px rgba(0,0,0,0.1); }
        .book-card h3 { margin: 0; color: #333; }
        .book-card p { color: #666; }
        .read-more { text-decoration: none; color: #007BFF; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Books in <%= category %> Category</h1>

        <% if (books.length > 0) { %>
            <% books.forEach(book => { %>
                <div class="book-card">
                    <h3><%= book.title %></h3>
                    <p><%= book.description %></p>
                    <a class="read-more" href="/book/<%= book._id %>">Read More</a>
                </div>
            <% }) %>
        <% } else { %>
            <p style="color: red;">No books found in this category.</p>
        <% } %>
    </div>
</body>
</html>
