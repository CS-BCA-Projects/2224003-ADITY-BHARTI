<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= categoryName %> Books</title>
    <style>
        /* Global Styles */
        body {
            font-family: Arial, sans-serif;
            background-color: rgb(255 241 196); /* Matches category page */
            text-align: center;
            margin: 0;
            padding: 0;
        }

        /* Page Container */
        .container {
            max-width: 900px;
            margin: 20px auto;
            padding: 20px;
            background: rgb(255 241 196);
            border-radius: 10px;
            box-shadow: 3px 3px 10px rgba(0, 0, 0, 0.2);
        }

        /* Heading */
        h1 {
            color: #333;
            font-size: 28px;
            text-transform: uppercase;
        }

        /* Book List */
        .book-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        /* Individual Book Card */
        .book-item {
            background: #fff;
            border-radius: 8px;
            padding: 15px;
            width: 250px;
            box-shadow: 2px 2px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease-in-out;
        }

        .book-item:hover {
            transform: scale(1.05);
        }

        .book-item h3 {
            color: #222;
            margin-bottom: 5px;
        }

        .book-item p {
            color: #555;
            font-size: 14px;
        }

        /* Read Now Button */
        .read-now {
            display: inline-block;
            padding: 8px 12px;
            margin-top: 10px;
            background: #ff4500;
            color: white;
            text-decoration: none;
            font-weight: bold;
            border-radius: 5px;
            transition: background 0.3s ease-in-out;
        }

        .read-now:hover {
            background: #d13600;
        }
    </style>
</head>
<body>
  
    <div class="container">
        <h1>Books in <%= categoryName %></h1>
        <div class="book-list">
            <% books.forEach(book => { %>
                <div class="book-item">
                    <h3><%= book.title %></h3>
                    <p>Author: <%= book.author %></p>
                    <a class="read-now" href="<%= book.contentUrl %>" target="_blank">Read Now</a>
                </div>
            <% }) %>
        </div>
    </div>

</body>
</html>
